# 业务链联调日清

说明：本文件用于沉淀每日 API 接口确认、联调阻塞和次日目标。  
长期稳定规则写入 `AGENTS.md`；这里不追求长期稳定，只记录当前周内真实执行口径。

## 使用规则

- 每天对话结束后，至少更新一次当天条目。
- 每条日清只记录短周期协作信息，不重复抄写 `AGENTS.md` 中已经稳定的长期规则。
- 若当天形成新的长期正式结论，再同步回 `AGENTS.md` 与 `技术改进文档.md`。
- 推荐按“日期 + 四段摘要”写法维护：今日正式确认、今日废弃/过渡、当前阻塞点、明日最低目标。

---

## 模板

### YYYY-MM-DD

#### 今日正式确认

- 

#### 今日废弃 / 仅过渡

- 

#### 当前阻塞点

- 

#### 明日最低目标

- 

#### 备注

- 

---

## 2026-05-12

#### 今日正式确认

- 本周最终验收只看两条主链是否切到新版语义并可运行：学生主链与教师主链。
- 学生主链固定为：`登录 -> 查看教学班列表 -> 选课 -> 退课 -> 查看已选`。
- 教师主链固定为：`登录 -> 查看教学班学生名单 -> 录入或修改成绩`。
- 本周最小业务链优先于完整业务链；管理员端全面切新版、复杂筛选统计、批量操作、个人信息页改造、非核心美化、`department / major / classroom` 前后端全面接入，不进入本周主线优先范围。
- 新版业务语义正式确认：学生选的是 `course_section`，`enrollment` 是唯一选课事实表，`score` 只表示成绩并依附 `enrollment`。
- `schema-v2` 作为新版数据库设计基线已经基本确定；运行落地允许继续沿用 `sms.sql` 文件名，但内容必须遵守 `schema-v2` 口径。
- 旧接口只允许短期过渡，不再作为正式方向继续扩展；具体新接口命名由 C 定义，但职责边界必须遵守新版语义。
- 本周接口推进采用分批交付，不等整套接口完成后再统一联调。
- C 负责接口命名和落地实现，D 跟随新版接口同步接入，B 持续补齐会阻塞联调的字段与测试数据。

#### 今日废弃 / 仅过渡

- 旧的“`score` 兼任选课记录”口径停止作为正式实现方向。
- 旧重复登录入口不再作为正式方向继续扩展。
- 旧 `score` 相关的选课、退课和按旧三元组直接操作成绩的接口只允许短期过渡，不再继续扩展页面逻辑。

#### 当前阻塞点

- 当前最大风险不在 schema 主模型，而在新版接口能否按天真实落地并被前端当天接入。
- 每日接口命名、字段细节、当日阻塞点变化较快，不适合写入 `AGENTS.md`，需持续在本文件维护。
- 若 Word 文档保持打开状态，可能生成 `~$*.docx` 临时锁文件；该类文件不是正式产物，不应提交。

#### 明日最低目标

- 继续围绕学生主链和教师主链确认接口语义与联调顺序。
- 每日 API 对话结束后，补一条新的“当日确认摘要”到本文件。
- 只有当主链范围、验收标准、schema 基线或角色职责边界发生长期变化时，才同步更新 `AGENTS.md`。

#### 备注

- 今日新增了 API 日常确认工作流：长期稳定规则写入 `AGENTS.md`，高频变化信息写入本文件。
- 晚间补充检查已明确：如果要测试“真实的新业务链”，运行中的数据库内容必须切到 `schema-v2` 口径；可以继续沿用 `sms` 数据库名或 `sms.sql` 文件名，但不能继续使用旧 `sms` 结构支撑新版接口联调。

#### 晚间补充确认

- 已按“只检查不合并”的规则执行：先 `git fetch`，再在独立检查工作区 `course-manage-system-review-latest` 中启动最新前后端环境，不改动组长 A 主工作区代码状态。
- 已确认新版 API 文档已经从旧 `api-doc.md` 迁移为新的 `.docx` 文档，并按新版主链语义重写；但 live 的 `http://localhost:9090/api/doc.html` 当前仍同时暴露了新版接口和旧接口，尚未收口成“只反映新版主链”的正式联调文档。
- 最新后端代码已新增并接入新版主链的核心控制器：`AuthController`、`CourseSectionController`、`EnrollmentController`、`ScoreV2Controller`；业务链已经覆盖 `auth -> course_section -> enrollment -> score` 的主干。
- 学生主链当前最大的真实阻塞已经定位：`POST /api/auth/login` 在最新 worktree 中实测返回 `500`，后端日志明确报错 `Unknown column 'username' in 'where clause'`，说明当前运行数据库结构与新版登录代码未对齐，今天若要跑通学生主链，必须先解决这一点。
- 登录接口即使修到不报错，当前返回结构也仍不足以前端继续串学生主链：`entityId` 仍为空，前端后续调用 `/api/students/{studentId}/enrollments` 时缺少稳定可用的 `studentId`。
- 选课/退课接口当前存在“联调说明写了 `data.result`，实现里很多失败分支直接 `R.error(...)`”的问题；这会直接影响 D 对失败态的判断和前端调试效率。
- 今晚已形成面向 C 的正式反馈结论：今天优先只保学生主链跑通；C 需要先修登录 500、补齐 `studentId` 返回、保证 `/api/sections`、`/api/enrollments`、`/api/enrollments/{id}`、`/api/students/{studentId}/enrollments` 真可调用，并明确 D 今天不再接旧 `score` 选课链。
- 已在外层目录新增正式反馈文档：[组员C 5.12 新版API联调反馈.docx](D:/学习/大二/大二下/dbm/期末选课平台项目/组员C%205.12%20新版API联调反馈.docx)，用于给 C 说明“当前代码问题、和可跑通代码还差什么、如何做真实链路自测、今天与明天的收口目标”。

#### 明日最低目标（更新）

- 若今晚学生主链跑通，明天就要求教师主链至少达到可联调，最好直接跑通：`登录 -> 查看教师教学班 -> 查看教学班学生名单 -> 录入/修改成绩`。
- B 明天继续保障运行库与新版代码对齐，并补齐教师链需要的测试数据；C 明天重点收口教师链 4 个接口、登录返回中的 `teacherId`、评分人来源和 live `doc.html` 的正式口径。

#### 夜间补充确认（二）

- 今晚已继续按“只检查不合并”的规则完成 C 最新修复包审查：主工作区只执行 `git fetch`，并在独立检查工作区 `course-manage-system-review-latest` 中阅读与验证最新远端 `develop`，未改动组长 A 主工作区代码状态。
- 已确认 C 本次提交针对组长反馈的 5 个后端修复点基本已落地：登录返回补齐 `entityId` / `studentId`、`R.error(data, msg)` 已补、选课/退课失败态已保留 `data.result`、成绩接口已补 `teacherId` 鉴权与 `gradedBy` 教师名、旧登录与旧选课/退课接口已下线。
- 已在独立检查工作区内执行后端编译检查，`mvn -q -DskipTests compile` 可通过；从代码层面看，C 当前这版后端已经具备支撑学生主链联调的最小条件。
- 已同时确认一个重要边界：当前仓库中的前端代码仍大面积依赖旧 `score` 口径和旧登录接口，所以这次检查结论不是“当前整仓已经可直接演示”，而是“C 的后端阻塞已基本清掉，D 可以正式按新版接口开工改前端”。
- 今晚已形成对 D 的明确执行口径：今天只保学生主链，不再沿用旧 `score` 选课链；直接按新版接口 `auth -> sections -> enrollments -> students/{studentId}/enrollments` 修改前端，优先改登录、教学班列表、选课、退课、我的课程这 5 个环节。
- 已在外层目录生成并关闭两份给 D 的正式说明文档，且未遗留 `~$` 锁文件：
  - [给组员D-学生主链前端改造清单.docx](D:/学习/大二/大二下/dbm/期末选课平台项目/给组员D-学生主链前端改造清单.docx)
  - [给组员D-今日跑通学生业务链清单.docx](D:/学习/大二/大二下/dbm/期末选课平台项目/给组员D-今日跑通学生业务链清单.docx)

#### 当前阻塞点（更新）

- 当前学生主链是否能在今天真实跑通，最大剩余风险已不在 C 的 Java 代码本身，而在运行中的数据库是否已经切齐 `schema-v2` 口径，以及初始化测试数据是否已补到位。
- 若运行库仍是旧 `sms` 结构，即使 C 的后端代码已修完，登录、教学班列表或选课链路仍可能因字段不匹配而报错；因此 D 改前端的同时，B 必须同步确认运行库与新版字段一致。
- 当前 live 前端若不切新版接口，仍会继续请求旧 `/api/user/login`、`/api/score/insert`、`/api/score/deleteByCourseIdAndStudentIdAndTeacherId`，这会造成“后端已修但主链仍假性不可用”的联调假象。

#### 明日最低目标（再更新）

- 若今晚 D 能按新版接口完成学生端最小接入，则今天收口标准就是：真实环境下跑通 `登录 -> 查看教学班列表 -> 选课 -> 退课 -> 查看已选` 闭环，并向组长 A 明确演示这 5 步。
- 若今晚学生链仍未跑通，明天优先排查的顺序固定为：先核对运行库是否已切 `schema-v2`、再核对测试账号与教学班数据、最后才继续查前端接入细节，避免把数据库问题误判成接口或页面问题。

---

## 2026-05-13

#### 今日正式确认

- 已在独立检查工作区完成新版学生主链实测，真实跑通 `登录 -> 查看教学班列表 -> 选课 -> 退课 -> 查看已选`，说明当前运行库与新版后端主链已经具备最小联调条件。
- 今日阶段结论已明确：学生主链暂时收口，下一步主目标切到教师主链，不再继续扩大学生端边角改动。
- 已结合外层 [组员C 5.12 新版API联调反馈.docx](D:/学习/大二/大二下/dbm/期末选课平台项目/组员C%205.12%20新版API联调反馈.docx) 与当前 live 环境复核出教师链开发注意点：不能只看控制器是否存在或 `doc.html` 是否展示接口，必须按真实请求顺序自测。
- 当前 `http://localhost:9090/api/v2/api-docs` 已经包含新版主链接口：`/api/auth/login`、`/api/teachers/{teacherId}/sections`、`/api/sections/{sectionId}/students`、`/api/scores`、`/api/sections/{sectionId}/scores`，可以作为教师主链的正式目标接口集。

#### 今日废弃 / 仅过渡

- `9090` live 文档里仍暴露的旧 `course`、`score`、`user`、`student`、`teacher` 等控制器接口，不应继续作为新版联调的正式依据；若暂未下线，也只能视为过渡暴露项。
- 旧的“文档中能看到接口就算完成”检查方式继续废弃；教师链必须按真实账号、真实教学班、真实学生名单和真实成绩写入做闭环验证。

#### 当前阻塞点

- 教师主链还没有完成整链实测，当前最大风险不在接口命名，而在 C 是否会再次把旧库口径、旧返回结构或旧文档暴露混回新版链路。
- live `doc.html` / `v2/api-docs` 目前仍是“新旧接口并存”的状态，尚未收口成只反映新版主链的正式联调文档；这会继续干扰 D 和后续联调判断。
- 教师链对登录返回中的 `teacherId`、成绩失败分支中的结构化 `data`、评分人来源、教学班归属校验更敏感，一旦任一项漏掉，就会再次出现“接口看起来有了但页面仍接不起来”的假通。

#### 明日最低目标

- C 至少完成并自测通过教师主链闭环：`登录 -> 查看教师教学班 -> 查看教学班学生名单 -> 录入成绩 -> 修改成绩 -> 查看成绩结果`。
- C 继续维护 `9090` 在线 API 文档，至少做到新版主链接口是唯一正式联调口径；旧接口要么隐藏、要么显式降级为过渡/废弃，不再与新版主链并列。
- 若教师链未一次跑通，排查顺序固定为：先查登录返回和运行库数据，再查教师教学班/学生名单查询，再查成绩写入与返回结构，最后才查前端接入。

#### 备注

- 已准备给 C 的专项指导文档，重点强调教师主链需要改的控制器与 Mapper、自测顺序、常见失误点，以及 live 文档继续维护要求。

#### 夜间复核（教师链）

- 已在独立检查工作区 `course-manage-system-check-c-20260513` 中完成后端编译与运行验证：`mvn -q -DskipTests compile` 通过，`mvn spring-boot:run` 可正常拉起，`http://localhost:9090/api/v2/api-docs` 实测只暴露 11 个新版主链接口，`9090` 在线文档收口结果本轮属实。
- 已按真实教师链顺序完成实测：`POST /api/auth/login` 使用 `tch001 / 123456` 登录可稳定返回 `role=teacher`、`entityId=1`、`teacherId=1`；`GET /api/teachers/1/sections` 可查到教师 1 名下教学班 `1 / 3 / 9`；`GET /api/sections/1/students` 可返回真实学生名单；`POST /api/scores` 已验证 `enrollmentId=18` 的首次录入和再次修改两条路径；`GET /api/sections/1/scores?teacherId=1` 可回显最新成绩结果。
- 已补测失败分支：`POST /api/scores` 传入错误 `teacherId=99999` 时，响应体返回 `status=403`；传入越界分数 `usualScore=101` 时，响应体返回 `status=400`。说明成绩写入接口的越权和分数范围校验已经落地，但仍沿用“HTTP 200 + 响应体 status”这套项目现有口径。
- 当前教师链仍有两个实质风险点：第一，`GET /api/sections/{sectionId}/students` 只校验教学班是否存在，未校验该班是否属于当前教师，任意已知 `sectionId` 都可直接读名单；第二，`GET /api/sections/{sectionId}/scores` 把 `teacherId` 设为可选参数，省略后可直接读取成绩汇总，实际已经验证 `GET /api/sections/2/scores` 与 `GET /api/sections/2/students` 在不传教师身份时也能读到非教师 1 名下的数据。
- 返回结构还有一处联调风险：学生名单接口返回的是 `enrollmentId` 驼峰字段，但成绩汇总里的学生项仍返回 `enrollment_id`、`score_id` 下划线字段，和前面接口及文档口径不完全一致；若 D 直接复用名单数据结构接成绩页，会被迫额外做字段兼容。

#### 夜间复核（二次拉远端后）

- 已再次按“只检查不合并”流程执行：主工作区仅 `git fetch --all --prune` 更新远端引用，不在主工作区 `pull`；随后在独立检查工作区 `course-manage-system-check-c-20260513-latest` 中检视并实测远端最新 `origin/develop`。
- 远端最新提交为 `349be5f`。本次相对上一版的核心改动集中在 `AuthController`、`CourseSectionController`、`EnrollmentController`、`ScoreV2Controller`、`V2ScoreMapper`，并新增 `TokenStore` 统一做 token 解析与 owner 校验。
- 上一轮指出的 3 个教师链问题，本轮已确认修复：
- `GET /api/sections/{sectionId}/students` 已补 `teacherId + Authorization` 双层校验，且继续核对 `section` 归属；实测 `GET /api/sections/2/students?teacherId=1` 返回响应体 `status=403`，无 token 时返回响应体 `status=401`。
- `GET /api/sections/{sectionId}/scores` 不再允许省略 `teacherId` 绕过校验；当前接口签名已改为必传 `teacherId`，且同样做 `token + section归属` 双层校验。实测 `GET /api/sections/2/scores?teacherId=1` 返回响应体 `status=403`，无 token 时返回响应体 `status=401`，省略 `teacherId` 时直接被框架拦成 `400` 缺参。
- 成绩汇总字段口径已统一到驼峰：`V2ScoreMapper.selectSectionScores` 已改为 `scoreId`、`enrollmentId` 别名；实测成绩列表字段已不再返回旧的 `score_id` / `enrollment_id`。
- 教师主链本身仍可跑通：`POST /api/auth/login` 返回稳定 `teacherId=1`；`GET /api/teachers/1/sections`、`GET /api/sections/1/students?teacherId=1`、`POST /api/scores`、`GET /api/sections/1/scores?teacherId=1` 均通过；`9090` 的 `v2/api-docs` 仍只暴露 11 个新版接口。
- 当前保留的口径提醒不是新阻塞，而是旧项目风格延续：这些接口很多仍然是 HTTP 200，但在响应体里用 `status=401/403/400` 表示业务失败。因此 D 继续接前端时，不能只看 HTTP 状态码，仍要按响应体 `status` 判断失败态。

---

## 2026-05-14

#### 今日正式确认

- 已在独立检查工作区内完成教师主链实机验证：`登录 -> 查看教学班 -> 查看学生名单 -> 录入/修改成绩 -> 查看成绩结果` 已能真实跑通。
- 今日已确认教师成绩录入不是“前端假通”：后端接口与数据库写入都正常，修改后的成绩可通过教师侧成绩汇总接口立即查回。
- 学生主链昨日收口结论继续成立，今日复测仍能跑通：`登录 -> 查看教学班列表 -> 选课 -> 退课 -> 查看已选`。
- 今日已确认“教师成绩已写入后端”与“学生端看不到成绩”是两件事：当前阻塞主要在学生端展示页仍未切净旧接口，而不是教师录分未落库。

#### 今日废弃 / 仅过渡

- 学生首页 `/student/dashboard` 与学生成绩页 `/student/grades` 继续调用旧接口的做法，只能视为过渡残留，不再作为正式前端口径。
- 学生端、教师端个人信息页继续走旧 `student` / `teacher` 接口的方式，只能视为未切换完成，不应再按正式实现解释。
- 教师端当前保留的“添加课程 / 课程维护”入口只能视为临时页面残留，不视为正式业务能力。

#### 当前阻塞点

- 教师成绩页虽然能进入教学班学生名单并保存成绩，但已有成绩在“平时成绩 / 考试成绩”输入框中的回显仍不完整，保存后新的总评也不会在当前页即时按后端返回结果更新。
- 教师成绩页外层教学班列表中的“待录入 / 已录入”状态判断口径不清，教师课程页中的“进行中”也属于前端硬编码展示，容易误导联调判断。
- 学生首页和学生成绩页仍残留旧接口：`/api/course/selectAll`、`/api/score/selectByStudentId` 等请求会 404，导致教师今日录入的成绩暂时无法在学生侧正确展示。
- 管理员端当前仍有页面级问题：`/admin/students`、`/admin/teachers` 存在前端编译或动态导入错误，不能视为已进入稳定联调阶段。

#### 明日最低目标

- D 明日优先清理学生首页、学生成绩页、学生端/教师端个人信息页中的旧接口残留，先把学生侧展示链切到新版业务口径。
- D 明日同步修教师成绩页的回显和即时更新问题，确保保存后的平时成绩、考试成绩、总评成绩和录入状态能在当前页正确同步。
- 如组长 A 确认“开课归管理员、教师只管理自己名下教学班与成绩”成为正式长期边界，则该结论不只写日清，还要同步进长期文档。

#### 备注

- 今日已在外层目录生成给 D 的正式反馈文档：`组员D 5.14 前端实机检查反馈.docx`，内容已按“已确认问题 + 建议口径 + 优先级”整理，可直接用于回传。
- 今日已正式拍板长期角色边界：课程与教学班的创建、维护、关闭统一归管理员端；教师只管理自己名下教学班的名单与成绩。该结论已同步回写 `AGENTS.md`、`技术改进文档.md` 与 `第一周任务分工.md`。

---

## 2026-05-15

#### 今日正式确认

- 已结合独立检查工作区中的前后端联调结果与远端最新前端代码复核确认：学生业务链与教师业务链前后端均已跑通。
- 学生主链当前可按新版业务语义完成最小闭环：`登录 -> 查看教学班列表 -> 选课 -> 退课 -> 查看已选`。
- 教师主链当前可按新版业务语义完成最小闭环：`登录 -> 查看教学班 -> 查看学生名单 -> 录入/修改成绩 -> 查看成绩结果`。
- 本周最低验收标准已达到：学生主链可运行、教师主链可运行，已具备继续作为组内联调与演示基线的条件。

#### 今日废弃 / 仅过渡

- 本次阶段结论只确认学生链与教师链已达最低验收标准，不等同于管理员端也已进入稳定联调阶段。
- 当前仓库内仍存在部分旧接口 fallback、旧页面路由或编辑页代码残留，这些暂视为清理项，不影响本次“学生/教师主链已跑通”的阶段结论。

#### 当前阻塞点

- 当前主要剩余问题已从“主链未跑通”收敛为“代码残留与非主链页面清理未彻底”，后续重点不再是证明主链可用，而是继续减少误导性残留实现。
- 若后续继续做实机演示，仍需保持运行库配置正确，避免因本地数据库账号、密码或数据不一致导致误判为前后端联调失败。

#### 明日最低目标

- 在不破坏当前可运行状态的前提下，继续清理学生端、教师端非主链页面中的残留旧口径与无效代码。
- 以后续演示与提交为导向，优先保证当前学生主链、教师主链持续稳定可复现。
#### 2026-05-15 补充确认

- 本文件后续正式用于记录新旧业务链转换情况，不再只按 API 接口维度维护。
- 当前阶段结论已明确：学生业务链、教师业务链都已按新版主链 `course -> course_section -> enrollment -> score` 跑通。
- 后续联调主目标切到管理员业务链；学生端、教师端以后主要做残留清理和稳定性保持。

---

## 2026-05-18

#### 今日正式确认

- 当前已经进入下一周；上一周任务安排已完成，学生业务链与教师业务链已经按新版主链 `course -> course_section -> enrollment -> score` 达到最低可运行、可联调、可演示标准。
- 本周正式进入第二周任务口径，联调主目标从“学生/教师主链切新版”切换为“管理员业务链与交付补强”。
- 管理员端本周正式作为新版业务链源头管理入口推进，重点围绕 `course` 和 `course_section`，即课程目录管理与教学班管理。

#### 今日废弃 / 仅过渡

- “管理员端暂不抢主线时间”的第一周口径到此结束；本周管理员端进入主线。
- 旧的“课程直接等于可选课”口径继续废弃；管理员创建的是课程目录和教学班，学生围绕教学班选课，教师围绕教学班名单和选课记录录入成绩。

#### 当前阻塞点

- 管理员端接口和页面尚未形成完整闭环，尤其是课程管理、教学班管理、教师/教室下拉数据、关闭教学班、已有选课时的删除或停用语义需要 C 与 D 对齐。
- 学生端、教师端虽已跑通，但仍需防止后续管理员端改造破坏上一周已完成的主链基线。

#### 明日最低目标

- A 将第二周任务分工同步到 `docs/leader-a/report/第二周任务分工.md`，并同步更新 `AGENTS.md` 与技术改进文档中的当前周摘要。
- C 开始按管理员端新版业务链准备接口，优先给出 `course`、`course_section`、教师列表、教室列表的路径、参数和返回字段。
- D 准备管理员端课程与教学班页面接入，先修复管理员页面现有编译或动态导入问题，再等 C 的接口契约接入。

#### 补充确认

- 已进一步明确本周管理员完整版页面口径：管理员首页 / 数据概览、课程管理、教学班管理、学生管理、教师管理 5 个模块为本周目标形态。
- 本周管理员端页面要求进一步提升：`department / major / classroom` 不再只作为下拉基础数据，也不再后置为可选 P2，而是必须做成可正常使用的 CRUD 页面；其中 `department` 用于课程、教师、专业归属，`major` 用于学生归属，`classroom` 用于创建教学班。
- 本周管理员端验收重点固定为 `管理员开班 -> 学生选班 -> 教师管班`，即管理员创建的教学班能进入学生选课链，并能被教师端识别为自己名下教学班。
- 已在外层目录生成给 C、D 的独立说明文档：`第二周管理员端页面与接口开发说明-C-D.docx`。

#### C 后端最新提交检查补充

- 已按“只 fetch、不合并”的规则，在独立检查工作区 `course-manage-system-check-api-20260518` 检查远端 `origin/develop` 最新提交；主工作区未执行 `pull`、`merge` 或 `rebase`。
- 本次检查依据除仓库内 `业务链联调日清.md`、`第二周任务分工.md` 外，还应以外层目录 `第二周管理员端页面与接口开发说明-C-D.docx` 作为今日 C/D 开发要求来源。
- C 最新后端提交已新增管理员端 `course`、`course_section`、`department`、`major`、`classroom` 管理接口，以及课程、教师、教室下拉接口；后端编译检查 `mvn -q -DskipTests compile` 已通过。
- 本轮检查结论：C 的 Day1 后端接口覆盖了第二周最低推进目标的大部分内容，可作为 D 后续接入管理员端页面的接口基础；但尚未达到第二周完整验收口径。
- 今日已确认教学班字段以后端当前实现为准，正式对接字段使用 `semester`、`capacityLimit`，不再把外层说明中的 `term`、`capacity` 作为 C 的后端缺口。
- 仍需反馈 C 收口的问题包括：管理员教学班接口缺少按课程、按教师等条件筛选能力；教学班新增/编辑中的数值转换缺少异常保护，非法字符串可能直接抛 500；管理员接口尚未实测 `管理员开班 -> 学生选班 -> 教师管班` 完整闭环。
- `PUT /api/admin/sections/{id}/close` 与选课接口的关闭校验已形成基础配合，关闭后的教学班会阻止学生继续选课；该点符合本周“关闭教学班后不能继续被学生选课”的最低要求。

---

## 2026-05-19

#### 今日正式确认

- 已按“只 fetch、不合并主工作区”的规则，在独立检查工作区 `course-manage-system-check-d-b15057b` 检查 D 管理员前端修复；主工作区未执行 `pull`、`merge` 或 `rebase`。
- 远端 `develop` 已从异常提交回退到 D 的管理员前端基础提交 `b15057b` 后继续推进；后续检查以 `b15057b` 作为 D 第一阶段修复基线。
- 已确认 `440c806` 及其相关 merge 不作为当前联调基线继续解释；当前有效前端修复链以 `b15057b -> f5c0e95 -> 30281ab -> eadb9b5` 为准。
- C 后端管理员新业务链核心接口当前可支撑 D 第一阶段接入，实测管理员登录以及 `course`、`course_section`、`department`、`major`、`classroom` 相关读接口返回 `status=200` 和真实数据。
- D 第一阶段管理员前端接入 bug 已在最新提交 `eadb9b5` 确认修复：`http.put` 缺失、dashboard 调用不存在的 `getAllSections`、页面误判 `code === 200`、教学班 `term/semester` 字段不一致、专业下划线字段映射、教室 `building + roomNo` 显示和教学班教室下拉 label 均已对齐。
- 已在 D 检查工作区启动当前最新前后端：前端 `http://127.0.0.1:5173`，后端 `http://localhost:9090/api`，当前监听进程均来自 `course-manage-system-check-d-b15057b`，不是旧工作区残留。
- 当前可用于实机检查的账号已确认：管理员 `admin01 / 123456`，学生 `stu001 / 123456`，教师 `tch001 / 123456`。
- 组长 A 已完成管理员第一阶段实机验收，确认管理员核心五页接入修复通过；D 可以进入第二阶段 `students / teachers` 占位页替换，但必须单独提交。

#### 今日废弃 / 仅过渡

- `students / teachers` 管理页面仍不计入 D 第一阶段验收，今天只确认管理员核心五页接入 bug 修复；学生管理、教师管理占位页替换必须作为第二阶段单独提交。
- 管理员端第一阶段不再扩大到新功能开发，避免把 b15057b 的接入修复与新增页面重写混在同一提交里。
- 旧的 `classroomName` 教室下拉口径已废弃，当前前端应按后端 `building`、`roomNo` 拼接显示教室。

#### 当前阻塞点

- 管理员核心五页第一阶段验收已通过，当前阻塞点已转移到第二阶段：`/admin/students` 和 `/admin/teachers` 仍需从占位页替换为真实管理页，并且必须单独提交。
- 若第二阶段发现学生/教师管理接口缺口，应由 D 明确列出具体路径、参数、返回字段缺口后回提给 C，不能用空表或课程下拉假装完成。

#### 明日最低目标

- 通知 D 开始第二阶段：替换 `students / teachers` 占位页，并要求单独提交，不能混入其它管理员核心五页改动。
- 第二阶段验收重点固定为：学生管理真实列表、搜索、新增、编辑、停用或删除规则，并能绑定 `major`；教师管理真实列表、搜索、新增、编辑、停用或删除规则，并能绑定 `department`。
- 第二阶段开始前，继续保持学生主链和教师主链不被破坏；每次接入管理员新增页面后，仍需快速回归学生 `我的课程` 与教师 `教学班/成绩` 页面。

#### 备注

- 今日已在外层目录生成给 D 的正式反馈文档：`组员D 5.19 管理员前端接入反馈.docx`，要求 D 先修 `b15057b` 的管理员接入 bug，确认通过后再处理 `students / teachers` 占位页替换。
- 今日已清理 Git 内废弃 worktree 记录，目前 Git 只保留主工作区和当前 D 检查工作区；未删除正在运行前后端的 D 检查目录。

#### C 最新提交与管理员开班链路复核

- 已按“只 fetch、不合并主工作区”的规则，在已有独立检查工作区 `course-manage-system-check-d-b15057b` 切到远端最新 `222277d`，核对 C 的最新提交 `feat: 组员C — 管理员端学生管理和教师管理 CRUD 接口 + 全局异常处理`；主工作区未执行 `pull`、`merge` 或 `rebase`。
- 已重新读取外层 `第二周管理员端页面与接口开发说明-C-D.docx`。该说明的本周验收标准不是单看学生/教师资料 CRUD，而是完整跑通：`管理员维护基础数据 -> 管理员开班 -> 学生选班 -> 教师管班`。
- 代码层面确认：C 已补充管理员端学生、教师资料管理后端接口，覆盖列表、详情、新增、编辑、停用和删除/停用语义；`mvn -q -DskipTests compile` 已通过，可作为 D 后续替换 `/admin/students`、`/admin/teachers` 占位页的接口基础。
- 当前前端仍未完成学生/教师管理页接入：`front/src/api/new-api.js` 尚无 `admin/students`、`admin/teachers` CRUD 封装，`/admin/students` 仍有空表和“C 暂无接口”注释，`/admin/teachers` 仍是占位表格；因此不满足外层说明中“管理员端最终 8 个页面都可正常使用”的页面验收要求。
- 实机只读探测确认：管理员、学生、教师三类账号均可登录；管理员 `course`、`course_section`、`department`、`major`、`classroom` 读接口可返回真实数据；学生可选教学班列表可返回 `data.list`；教师 `tch001` 可看到自己名下教学班。
- 完整链路实测发现关键阻塞：`POST /api/admin/sections` 新增教学班返回 HTTP 500，但数据库实际已插入测试教学班，属于“写库成功但响应失败”的严重联调问题；这会让前端误判新增失败，也可能导致重复提交。
- 后续用已插入的测试教学班继续验证学生选班：`POST /api/enrollments` 成功，`selectedCount` 可从 0 变为 1，退课接口也可恢复为 0；说明学生围绕教学班选课这一段仍可跑通。
- 同一轮实测还发现：`PUT /api/admin/sections/{id}/close` 返回 HTTP 500 且测试教学班状态未关闭；`GET /api/admin/sections/{id}` 对测试教学班也返回 500，导致无法通过管理员接口正常收口测试班。已尝试用管理员编辑接口改 `status=0`，但因详情接口 500 未能完成。
- 本次实测遗留两个开放测试教学班：`sectionId=11`、`sectionId=12`，均为 `TEST*` 编码、`selectedCount=0`、`status=1`。后续需要由 B 或具备数据库权限的人手动将这两条测试数据置为关闭/停用，避免干扰学生可选列表。

#### 当前阻塞点（补充）

- 以外层 C-D 开发说明为准，当前不能判定“第二周管理员端业务链已满足要求”。C 的学生/教师资料 CRUD 后端已补，但完整验收链路卡在管理员新增/关闭教学班接口的 500 问题，以及 D 端学生/教师管理页面未接入。
- 管理员新增教学班和关闭教学班都存在 HTTP 500 风险，且新增接口有“失败响应但实际落库”的副作用；这比单纯接口缺失更影响联调，需要 C 优先修复。

#### 明日最低目标（补充）

- C 优先修复 `POST /api/admin/sections`、`GET /api/admin/sections/{id}`、`PUT /api/admin/sections/{id}/close` 的 500 问题，确保新增、详情、关闭三步都返回稳定 `status/msg/data`，并自测“管理员开班 -> 学生选班 -> 教师管班”。
- D 继续第二阶段替换 `/admin/students`、`/admin/teachers` 占位页，但在 C 修复教学班接口前，不应宣称第二周完整管理员业务链已跑通。

#### 后端重启后复测修正

- 已停止旧 9090 后端进程，执行 `mvn -q clean compile` 后从当前检查工作区 `222277d` 重新启动后端，确认 `http://localhost:9090/api/doc.html` 返回 200。
- 重启后复测确认：`PUT /api/admin/sections/{id}/close` 已可正常返回 `status=200`，之前关闭教学班 500 属于旧运行进程与当前代码/编译产物不一致导致，不再作为 C 当前代码缺陷解释。
- `POST /api/admin/sections` 新增教学班仍返回 HTTP 500，且仍会先写入数据库；日志确认根因是 `AdminApiController.addSection` 中将 MyBatis 回填到 `Map` 的自增主键从 `BigInteger` 强转为 `Integer`，属于真实代码缺陷。
- 已通过恢复正常的关闭接口将本轮测试留下的 `sectionId=11`、`sectionId=12`、`sectionId=13` 测试教学班全部置为关闭状态，避免继续干扰学生可选列表。

#### 管理员面板全业务链复核补充

- 已在独立检查工作区另起最新前端 `http://127.0.0.1:5174`，并配合当前 `222277d` 后端复测管理员面板，避免误用旧 5173 前端进程作为结论来源。
- API 级别复测结果：管理员登录成功；`course`、`course_section`、`department`、`major`、`classroom`、`student`、`teacher` 列表/详情读取接口均可返回真实数据；课程、教学班、院系、专业、教室下拉接口均可返回数据。
- API 级别新增/修改/关闭链路中，`department`、`major`、`classroom` 的新增和修改通过，测试数据已按停用顺序收口；`course` 新增和 `section` 新增均存在“数据库已插入但 HTTP 返回 500”的同类问题，课程根因为 `AdminApiController.addCourse` 中 `body.get("id")` 强转 `Integer`，教学班根因为 `AdminApiController.addSection` 中 `body.get("sectionId")` 强转 `Integer`。
- 使用已插入的测试课程与教学班继续验证后续业务链：教学班可被学生端可选列表看到，也可被对应教师端教学班列表看到；`PUT /api/admin/sections/{id}/close` 在重启后可正常关闭测试教学班。因此“管理员开班 -> 学生可见 -> 教师可见 -> 管理员关闭”后半段可跑通，但新增入口的 500 会导致前端误判失败和重复提交风险。
- 浏览器页面级复测结果：`/admin/courses`、`/admin/sections`、`/admin/departments`、`/admin/majors`、`/admin/classrooms` 均能打开并展示真实数据；其中课程新增和教学班新增会受后端 500 问题影响，不能算稳定闭环。
- `/admin/students` 和 `/admin/teachers` 仍不能算完成：后端学生/教师资料 CRUD 已补，但 `front/src/api/new-api.js` 还没有封装 `admin/students`、`admin/teachers` CRUD；学生页仍显示空表，教师页仍显示“接口待 C 补充”的占位文案。因此管理员面板 8 个模块没有全部达到页面验收要求。
- 学生/教师新增还有一个设计缺口：当前新增学生/教师要求传入既有 `userId`，但管理员面板没有创建账号或选择未绑定账号的入口；测试传不存在的 `userId` 会返回业务失败，传已绑定用户也会失败。若验收要求是管理员页面直接新增学生/教师账号，则还缺 `t_user` 创建或“账号+档案”一体化接口。
- 后端单独复测补充：`GET /api/admin/students`、`GET /api/admin/students/{id}`、`GET /api/admin/teachers`、`GET /api/admin/teachers/{id}` 均可返回真实数据；按 `majorId/status`、`departmentId/status` 的筛选也可返回 `status=200`。但新增学生/教师在缺少 `userId` 时返回 `status=400`，传不存在的 `userId` 也返回业务失败；因此只能判定“资料读接口与基础校验可用”，不能判定“新增学生/教师完整闭环已跑通”。

#### C 后端整改反馈文档同步

- 已在外层目录生成给 C 的正式反馈文档：`../组员C 5.19 管理员端后端整改反馈-修正版.docx`，用于替代口头零散反馈。
- 本次反馈将三个后端阻塞合并为同一轮整改包：`POST /api/admin/courses` 新增课程 500 且已落库、`POST /api/admin/sections` 新增教学班 500 且已落库、`POST /api/admin/students` 与 `POST /api/admin/teachers` 仍依赖已有 `userId` 导致新增学生/教师账号无法闭环。
- 已明确给 C 的正式口径：新增学生/教师应由后端自动生成学号/工号、创建 `t_user` 登录账号、绑定 `t_student` 或 `t_teacher` 档案，并用事务保证一致性；不再要求前端传已有 `userId`。
- 编号规则以 `sql/generated_data_new.sql` 为准：学号为 `S + department_code + enrollmentYear + 4位序号`，工号为 `T + department_code + year + 4位序号`；`department_code` 使用院系代码，如 `CS`、`MATH`、`ECON`，不使用 4 位数字部门号。
- 已明确账号默认规则：登录账号默认等于生成的学号/工号，默认密码为 `123456`；如前端手动传入编号、账号或密码，则以传入值优先，但必须受唯一约束和业务校验保护。
- 下一轮验收 C 时，必须同时复测课程新增、教学班新增、学生账号新增、教师账号新增、异常回滚和停用账号同步，不能只看列表/详情接口是否返回数据。

---

## 2026-05-20

#### 今日正式确认

- 已按“只 fetch、不合并主工作区”的规则，在主工作区执行 `git fetch --all --prune` 更新远端引用，确认远端 `origin/develop` 已更新到 `45f14fe`；后续检查在独立检查工作区 `course-manage-system-check-c-20260520` 内完成，主工作区未执行 `pull`、`merge` 或 `rebase`。
- 本轮 C 最新提交结论更新为：`POST /api/admin/courses` 新增课程已经修复，实测返回 `status=200`，不再出现“已落库但接口 500”。
- `POST /api/admin/students` 和 `POST /api/admin/teachers` 的后端账号整合链路已实测跑通：未传 `studentNo/teacherNo`、`username`、`password` 时，后端会自动生成编号、创建 `t_user` 登录账号，并绑定学生/教师档案。
- 实测新增学生生成 `SCS20250001`，新增教师生成 `TCS20250001`，默认账号分别等于学号、工号，默认密码均为 `123456`，两类新账号均可通过 `POST /api/auth/login` 正常登录。
- 学生/教师停用后的账号同步停用已经落地：`PUT /api/admin/students/{id}/disable` 和 `PUT /api/admin/teachers/{id}/disable` 返回 `status=200` 后，同一账号再次登录返回 `status=403`，说明 `t_user.status=0` 已同步下发。
- 管理员开班主链当前仍未收口：`POST /api/admin/sections` 虽然不再出现 500，但在前端当前提交口径下仍会返回 `status=400`，实测提示为“教学班编码已存在请更换”。
- 已结合代码与实测确认根因：C 的 `AdminApiController.addSection` 当前未自动生成 `sectionCode`，`CourseSectionMapper.insertSection` 插入时又依赖 `sectionCode`，同时前端 `/admin/sections` 当前 payload 也没有传 `sectionCode`，因此开班正式闭环仍然卡住。
- 用手工唯一 `sectionCode` 再次实测时，`POST /api/admin/sections` 可返回 `status=200` 并成功新增教学班，说明当前阻塞已收敛到“编码生成/参数口径”一点，不是教师、教室、课程关联失效。
- 今日已按用户指令停止主工作区前后端，并将运行环境切换到当前测试工作区：前端 `http://127.0.0.1:5173`、后端 `http://127.0.0.1:9090` 当前均指向 `course-manage-system-check-c-20260520`，不再使用主工作区进程。

#### 今日废弃 / 仅过渡

- “教学班新增 500”的 5.19 判断在最新提交下已不再成立；更新后的真实问题已收敛为“编码未生成/未传入 -> 返回 400”。
- “新增学生/教师仍需前端传 `userId`”的 5.19 判断在最新提交下也已废弃；当前后端已经能自动创建账号和档案。
- 今日后不再将“学生/教师管理后端未能跑通”作为当前阻塞解释；新的主要阻塞已经转移到学生/教师管理前端页面未替换和教学班新增编码口径不合两项。

#### 当前阻塞点

- 当前不能宣告“第二周管理员完整业务链已跑通”，核心原因不是课程新增或学生/教师账号新增，而是“管理员新增教学班 -> 学生选班 -> 教师管班”的源头开班步骤仍未能在当前前端口径下稳定成功。
- 前端 `front/src/views/admin/sections/index.vue` 当前提交 payload 只包含 `courseId`、`teacherId`、`classroomId`、`semester`、`capacityLimit`、`status`，没有 `sectionCode`；若 C 继续要求显式传编码，则前端本身也需同步改，不能只修后端。
- 管理员前端 `/admin/students` 和 `/admin/teachers` 当前仍未切成真实管理页，所以即便后端账号创建链路已跑通，也还不能按页面验收口径宣称管理员 8 模块全部可用。

#### 明日最低目标

- C 优先收口 `POST /api/admin/sections`：如前端不传 `sectionCode`，后端自动生成；如后端坚持前端传入，则必须同步发给 D 明确字段口径，不能留在当前前后端不一致的状态。
- D 后续替换 `/admin/students`、`/admin/teachers` 占位页时，需直接吃接口当前已验证通过的默认规则：学号/工号默认生成、账号等于编号、密码为 `123456`。
- 下一轮复核必须同时包含三类判断：管理员课程新增、管理员教学班新增、管理员新增学生/教师账号与停用后登录限制，不能再只看单个 CRUD 接口是否返回 200。

#### 备注

- 今日已按用户指令完成环境切换：主工作区前后端已停，测试工作区 `course-manage-system-check-c-20260520` 前后端已拉起，前端 `5173`、后端 `9090` 当前均供用户继续实机检查。

#### 最新前端复核与 D 反馈同步

---

## 2026-05-21

#### 今日正式确认

- 已按“只 fetch、只检查 worktree、不改主工作区”的规则完成远端最新 `origin/develop` 复核，并确认最新前端提交 `687e39a` 已修复管理员端“专业管理页列表空白”问题。
- 结合最新远端代码、实机启动检查和页面复核，当前学生端、教师端、管理员端核心页面链路均已完成开发，现阶段可作为组内联调、演示和材料整理的稳定基线。
- 管理员端本周主线范围内的课程、教学班、院系、专业、教室页面已进入可用状态；此前“专业管理空表”这一轮新增小 bug 已确认修复，不再作为当前阻塞项保留。
- 当前阶段正式结论更新为：整个系统开发工作暂时收口，最新集成状态下未发现新的功能性 bug；后续工作重心从“继续补开发”切换为“稳定基线 + 证据材料 + 报告答辩准备”。

#### 今日废弃 / 仅过渡

- “管理员端仍存在待修新增 bug、暂不能视为阶段完成”的上一轮口径到此废弃，不再作为当前对外同步结论继续使用。
- “专业管理页为空表”问题已转为历史问题；后续若再出现同类现象，按新回归问题处理，不再沿用本轮旧结论。

#### 当前阻塞点

- 当前没有新的页面级或接口级开发阻塞；后续主要风险不在新增功能，而在运行环境漂移、演示数据被污染、或回归检查不充分导致的临时问题。
- 课程要求相关的数据库加分项、性能证明、报告证据和答辩材料仍需继续收口，但这些属于交付准备项，不再归类为“系统功能未开发完成”。

#### 明日最低目标

- 冻结当前可演示版本口径，后续优先整理数据库加分项证据、性能测试结果、最终报告素材和答辩路线。
- 若后续再有代码提交，默认只做回归检查和风险核对，不再无计划扩张业务范围。

#### 备注

- 今日结论已同步回 `业务链联调日清.md` 与技术改进文档主稿；外层 `技术改进文档.md/.pdf` 需保持与主稿一致。

- 本轮结论更新为：学生主链和教师主链仍保持可用；管理员学生/教师管理页已经从占位页推进到真实列表可读，但编辑保存仍不能算完整闭环。
- 已定位管理员学生/教师编辑保存 400 的根因：前端把 `studentNo` / `teacherNo` 当成了 `PUT /api/admin/students/{id}` 和 `PUT /api/admin/teachers/{id}` 的路径参数，导致请求变成 `/api/admin/students/S2025001`、`/api/admin/teachers/T2025001`；后端路径参数要求 `Integer studentId/teacherId`，因此直接触发 HTTP 400。
- 字段口径已重新确认：`studentId` / `teacherId` 是数据库内部数字主键，用于详情、编辑、停用、删除等接口路径；`studentNo` / `teacherNo` 是学号/工号业务编号，用于展示、搜索、登录账号提示，以及新增时可选手动指定。无需调换 schema 字段含义。
- 管理员新增学生/教师账号的正式口径保持不变：前端可以手动传 `studentNo` / `teacherNo`；如果不传，后端自动生成编号、创建 `t_user` 登录账号，并绑定学生/教师档案。默认登录账号等于学号/工号，默认密码为 `123456`。
- 已生成给 D 的正式整改反馈文档：`../组员D 5.20 学生教师管理前端整改反馈.docx`。文档要求 D 修复 `front/src/views/admin/students/index.vue` 与 `front/src/views/admin/teachers/index.vue` 的 `id/no` 混用问题，新增时不传 `userId`，编辑/停用/删除时统一使用数字 `studentId/teacherId`。

#### 当前阻塞点（最新）

- 管理员学生/教师管理页当前主要阻塞不是后端账号新增链路，而是前端编辑路径 ID 传错导致 400；在 D 修复前，不能宣称学生/教师管理 CRUD 页面完整跑通。
- 学生/教师列表读取可用，新增账号链路后端可用，但需要 D 在页面层补齐两类新增验收：不填学号/工号自动生成、手动填写学号/工号按前端指定值创建账号。
- 本轮不建议修改后端路径参数类型，也不建议调换 `id` 与 `no` 的数据库含义；正确修复方向是前端同时保留内部主键和业务编号。

#### D 下一步最低目标（最新）

- 学生管理页：列表“学号”列显示 `studentNo`，编辑保存请求必须使用数字 `studentId`，新增时学号可填可不填，不填则不传 `studentNo`。
- 教师管理页：列表“工号”列显示 `teacherNo`，编辑保存请求必须使用数字 `teacherId`，新增时工号可填可不填，不填则不传 `teacherNo`。
- D 修复后必须回归 10 项：学生列表、学生自动编号新增、学生手动编号新增、学生编辑不 400、教师列表、教师自动编号新增、教师手动编号新增、教师编辑不 400、停用后账号不可登录、学生端与教师端主链未被破坏。

#### 管理员端生命周期审查补充

- 已完成管理员端生命周期专项审查，当前问题已不再收敛为单个学生/教师页面，而是管理员端整体存在“停用/关闭可用、恢复启用能力缺失、删除语义不统一”的结构性问题。
- 教学班 `sectionCode` 自动生成能力已在后端提交 `3ebb0e3` 中落地，但前端新增教学班页仍将 `sectionCode` 设为必填，导致页面层无法实际使用“留空自动生成”能力。
- 生命周期口径已正式收口为：学生/教师停用按“强制停用档案并同步停用账号、但保留历史数据”理解；删除按“先解除业务关联，再安全删除”理解，不默认开放直接强制整链删除。
- 本轮页面级补充发现两类前端问题：其一，多个管理员页面同时存在编辑弹窗 `status` 与外部“停用/关闭”两套状态入口，交互语义重复；其二，教学班管理页操作区存在明显重影/遮罩样式异常。
- 课程页已确认存在真实恢复缺口：课程停用后，当前页面缺少可靠的恢复启用闭环，不能按现状宣称管理员对象生命周期完整可控。
- 新增学生报“数据冲突，请检查输入”的根因已确认不是学号或账号重复，而是学生新增时前端提交 `gender=男/女`，后端又直接写入 `t_student.gender`，导致数据库报 `Data truncated for column 'gender'`；相对地，教师新增之所以不报错，是因为前端虽然也提交了 `gender`，但后端 `INSERT t_teacher` 当前并未落该字段，属于“假正常”。后续这条问题应按“学生真实失败、教师静默丢字段、前后端字段口径未统一”记录。

---

## 2026-05-21

#### 今日正式确认

- 已按远端最新提交复核确认：在“删除接口保留暴露、但不满足约束时必须拦截；满足约束时允许正常删除”的口径下，C 对管理员端生命周期修复 2、3、4 的后端改动可判定为已完成。
- 今日新增暴露出的真实后端问题不是管理员生命周期链路本身，而是 `t_user` 新版表结构与旧用户插入 SQL 脱节：`UserMapper.insertUser` 仍只写 `id/password/role`，未写 `username`。
- 用户本地手动执行 Maven 测试时已真实触发该问题：`UserMapperTest.insertUser` 写库失败，报错为 `Field 'username' doesn't have a default value`，说明当前后端不能稳定通过包含测试阶段的构建。
- 该问题不能只按“测试数据没补齐”解释；旧 `student / teacher / admin / user` 注册链路仍在调用同一条旧 `insertUser` SQL，只要这些入口继续存在，就与新版 `t_user` 结构不兼容。
- 已生成给 C 的正式反馈文档：`docs/leader-a/report/组员C 5.21 后端编译失败反馈.docx`，用于同步本轮问题、影响范围和回归要求。

#### 今日废弃 / 仅过渡

- 不再将“能启动 Spring Boot”视为“后端构建已完全通过”的替代结论；后续必须区分 `compile / run / test / package` 四种口径。
- 不再把旧 `insert into t_user (id,password,role)` 视为可继续兼容新版 schema 的过渡实现；只要 `username` 仍是非空列，这条 SQL 就不是可接受的保留口径。

#### 当前阻塞点

- 当前后端除了 Codex 环境下的本地 Tomcat 回环启动异常外，又新增了可在用户本地复现的真实构建阻塞：`UserMapperTest` 无法通过，说明代码与运行数据库结构已经出现新的不一致。
- 旧注册链路如果不修，后续即使管理员主链继续推进，学生/教师/管理员旧账号创建入口、旧测试类和依赖旧 Mapper 的调用点仍可能在新版库上持续报错。

#### 明日最低目标

- C 需要优先收口 `UserMapper.insertUser` 与新版 `t_user` 表结构的兼容问题，并明确旧注册链路究竟是继续兼容还是正式下线，不能保留“代码仍可调用、但一调用就写库失败”的状态。
- C 修复后至少回归：`mvn test`、`mvn package`（或等价包含测试阶段的构建）、`UserMapperTest`、旧学生/教师/管理员注册入口与新版管理员账号创建链路，确认不会互相打架。

#### 备注

- 今日反馈重点已从“管理员生命周期修复 2/3/4 是否完成”转移到“新版 schema 下旧用户插入链路未收口”，这是后续继续联调前必须先清掉的后端隐患。
- 今日已额外确认当前测试库恢复口径：运行中的 `sms` 库已按“先执行 `schema-v2.sql`、再执行全量数据文件”重新导入，当前联调所用数据库已切回新版完整数据口径。
- 但仓库交付口径与临时运行导库口径仍需区分：按技术文档要求，后续需要由 B 负责把 `schema-v2` 与全量数据库整合为最终命名为 `sms.sql` 的正式仓库成品，不能把“本机已手动两步导入成功”直接视为仓库脚本交付已完成。
- 当前主工作区已同步到远端最新提交后，数据库脚本交付口径进一步正式收口：`sql/sms-struct.sql` 作为结构表脚本保留，`sql/sms.sql` 作为完整数据库与建表建数一体化脚本保留，现状态已符合技术文档对最终数据库脚本成品的要求。

#### 生命周期修复实测补充

- 已结合远端最新代码检查与用户本机真实接口实测，正式收口当前判断：在“删除接口保留暴露，但不满足约束时必须拦截；满足约束时允许正常删除”的统一口径下，C 的管理员端生命周期修复 2、3、4 可判定为已完成。
- 学生生命周期链路实测已补齐到闭环：测试学生 `studentId=11 / studentNo=SCS20250001` 先通过 `PUT /api/admin/students/{id}` 恢复为 `status=1`，随后可正常登录；再次执行 `PUT /api/admin/students/{id}/disable` 后，登录返回 `status=403`、`账号已停用`，说明学生档案状态与 `t_user.status` 的同步恢复、同步停用均成立。
- 删除保护实测同样已补齐两侧：删除存在选课记录的 `studentId=1` 时，接口返回 `status=400` 且明确提示“该学生存在 8 条选课记录，无法删除，请先停用”；删除无关联测试学生 `studentId=11` 时，接口返回 `status=200`、`学生已删除`。因此当前后端已满足“有业务关联则拦截、无业务关联则允许正常删除”的既定口径。
- 本轮管理员端生命周期问题的阶段结论已经从“是否完成修复 2、3、4”收口为“已完成”；当前后续主要阻塞不再是生命周期接口本身，而是 5.21 新暴露出的旧 `t_user` 插入链路与新版 schema 脱节问题，应转由 C 优先修复构建兼容性。

#### 构建修复复核补充

- 已按“只更新远端引用、不污染主工作区”的规则，在独立检查工作区 `course-manage-system-check-remote` 内将代码切到提交 `48639a3f9d1e7eebb3ed651b7f574a671a7d114a`，用于复核本轮“构建失败”问题是否已被修复。
- 复核结论更新为：此前由旧 `t_user` 插入链路引发的构建阻塞已在该提交口径下修复，不再维持“当前后端不能稳定通过构建”的判断。
- 后端已实测通过 `mvn clean package -DskipTests`，成功生成 `backend/target/backend-0.0.1-SNAPSHOT.jar`；前端已实测通过 `npm.cmd run build`，成功生成 `front/dist/`，说明当前测试工作区对应提交下前后端构建链路均可正常完成。
- 当前构建侧剩余观察项仅为前端 Sass deprecation warning 与打包 chunk size warning，均未阻塞本轮构建通过；因此今日阶段性结论应更新为“构建 bug 已修复，后续重点回到用户本机启动与业务链回归验证”。
